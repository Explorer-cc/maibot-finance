# 待核验问题清单

本文件记录当前实例日志和配置审查中发现、但尚未采取运行期修改的问题。记录日期为 2026-08-08。不得在此写入 API 密钥、Token、Cookie、QQ 登录态、群号或聊天正文。

## 运行可用性

### Q1：部分模型任务没有可用候选模型

- 现象：日志出现“所有 0 个模型均尝试失败”及“生成内容时出错：请求失败，所有可用模型均已尝试失败”。
- 已知事实：`learner`、`expression_use`、`emoji`、`voice` 等任务的模型列表为空；这与已配置但请求失败的模型供应商错误不同。
- 影响：任何调用这些任务的核心功能或插件都会失败；仅凭现有日志不能确定具体调用方。
- 待核验：分别确认各任务是否应当启用，以及现有模型提供商和模型是否支持其所需能力。
- 可能处置：为确有需要的任务配置已验证的候选模型，或关闭会触发空任务的功能。两种处置都会改变运行行为，须先获得明确授权。

### Q2：NapCat 与 Core 的 WebSocket 链路曾中断

- 现象：日志记录 NapCat 适配器连接 `ws://napcat:3001` 已结束，之后多次重连失败；同一时段可见 QQ 离线迹象。
- 已知事实：容器状态为 `Up` 或 `healthy` 只能证明容器及健康检查通过，不能证明 QQ 登录态和 Core-NapCat 消息链路可用。
- 影响：中断期间无法保证收发消息；该问题与 WebUI 的插件 manifest 扫描告警没有直接因果关系。
- 待核验：下次发生时同时检查 NapCat 的 QQ 登录状态、NapCat 容器日志、Core 日志及容器内 `3001` 监听/连通性；避免输出登录凭据或二维码。

### Q3：异步任务管理器清理不存在任务的警告

- 现象：停止或重连期间出现尝试移除不存在的统计数据输出、遥测心跳和遥测统计上传任务的警告。
- 已知事实：当前没有证据表明它导致消息收发失败或数据损坏。
- 待核验：确认其是否只发生在 Core 生命周期切换期间；如持续发生，再结合当前 MaiBot 版本的上游 issue 或日志定位。

## 插件目录与持久化

### Q4：WebUI 反复报告插件目录缺少 `_manifest.json`

- 现象：WebUI 对多个点号命名目录报告缺少 `_manifest.json`，包括内置插件、NapCat Adapter 及若干第三方插件。
- 已知事实：对应的实际插件代码目录通常为下划线命名且包含 manifest；同时存在点号命名的数据目录。WebUI 扫描到两类目录，因此产生告警。已启用的每日群聊分析插件有实际运行日志，说明该告警不等于插件未加载。
- 影响：告警噪声会掩盖真正故障；目前未证实影响插件执行。
- 待核验：确认上游当前版本是否已修复扫描范围；在不改动生产数据的前提下，确定数据目录与代码目录的官方布局。
- 约束：不得为消除告警直接删除、移动或重命名 `runtime/` 下的插件目录；这可能破坏插件数据或下次启动。

### Q5：`github.zfgy.deer-pipe-table` 使用旧式数据目录

- 现象：插件运行时提示旧目录 `/MaiMBot/plugins/github_zfgy_deer-pipe-table/data`，建议迁移至统一目录 `/MaiMBot/data/plugins/github.zfgy.deer-pipe-table`。
- 影响：后续插件升级、目录扫描或持久化约定可能不兼容；目前没有证据表明已有数据已损坏。
- 待核验：先确认该插件是否实际启用、现有数据是否有价值，以及上游提供的迁移步骤是否会保留全部数据。
- 约束：迁移属于数据变更，必须先做可验证的副本并经运营者明确授权；不应与镜像升级混合执行。

## 已启用插件的边界问题

### Q6：照片 EXIF 定位插件的隐私与外发风险

- 已知事实：插件会读取群内图片和文件的 GPS EXIF；命中后将坐标发送至外部逆地理编码服务、在群内回复地址，并把坐标与地址写入 Core 日志。
- 影响：这超出最小化数据处理和日志脱敏边界，且影响发图成员的位置信息。
- 待决：是否继续保留该插件；如保留，是否需要明确群规则、收紧触发范围或调整日志处理。任何禁用或配置调整都需明确授权。

### Q7：每日群聊分析插件的作用范围与模型依赖

- 已知事实：插件已启用自动汇总；空 `target_chats` 按插件实现表示所有可见活跃群。目前 Adapter 单群白名单限制可见范围，但插件本身未重复限制。日志已显示其执行过 LLM 调用，且出现超时或空结果。
- 影响：未来若 Adapter 白名单扩大，分析范围会随之扩大；模型失败会降低摘要可靠性。
- 待决：是否将目标群显式限定为当前唯一允许群，并确认其模型超时、重试和失败输出策略。

### Q8：联网搜索、Pixiv 与智能戳一戳的范围

- 联网搜索：启用网页抓取和多个搜索后端，会使回答可能引用外部网页内容，不能将实例描述为无联网能力。
- Pixiv：启用外部图片下载；聊天过滤关闭。色图群白名单为空，私聊策略允许色图，但当前 Adapter 私聊空白名单阻断了普通私聊。
- 智能戳一戳：群聊、私聊响应、旁观反应和主动戳一戳均启用；没有独立群白名单，依赖 Adapter 的外层限制。
- 待决：逐项确认这些行为是否仍符合当前群的预期，并在需要时为插件建立独立、显式的作用域限制。

### Q9：第三方插件的权限与供应链风险

- 已知事实：插件代码目录以读写方式挂载给 Core。第三方 Python 插件可读取 Core 容器内可见的运行文件，并具备网络访问能力；`plugin.permission` 中的 QQ 标识所能授予的实际动作尚未核验。
- 影响：插件安装、升级或启用均可能改变数据流和执行权限。
- 待核验：审查每个已安装插件的来源、版本、manifest、依赖、网络行为和权限语义；不要在审查记录中复制私有配置值。

## 处理顺序建议

1. 优先定位 Q2，确认 QQ 登录和 `ws://napcat:3001` 链路稳定性。
2. 决定 Q6 至 Q8 中哪些插件应继续保留，再收紧其范围。
3. 处理 Q1 的空模型任务，避免无候选模型的功能反复失败。
4. 最后处理 Q4 与 Q5 的目录布局和数据迁移，先核验官方方案与数据完整性。

以上均为待确认事项，不构成已执行的变更，也不要求重启服务。
